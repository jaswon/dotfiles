#!/bin/bash

case $1 in
  +)
    pactl set-sink-mute 0 0
    pactl set-sink-volume 0 +10%
  	;;
  -)
    pactl set-sink-mute 0 0
    pactl set-sink-volume 0 -10%
  	;;
  %)
  	# Toggle mute
    pactl set-sink-mute 0 toggle
  	;;
esac
